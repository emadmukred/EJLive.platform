<Project Sdk="Microsoft.NET.Sdk">
  <ItemGroup>
    <ProjectReference Include="..\EJLive.Core\EJLive.Core.csproj" />
    <ProjectReference Include="..\EJLive.Core.UI\EJLive.Core.UI.csproj" />
    <ProjectReference Include="..\EJLive.Server\EJLive.Server.csproj" />
    <ProjectReference Include="..\EJLive.Shared\EJLive.Shared.csproj" />
  </ItemGroup>
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <UseWindowsForms>true</UseWindowsForms>
    <ImplicitUsings>enable</ImplicitUsings>
    <EnableDefaultItems>false</EnableDefaultItems>
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
    <AssemblyName>EJLive.Server.WinForms</AssemblyName>
    <RootNamespace>EJLive.Server.WinForms</RootNamespace>
      <EnableDefaultCompileItems>false</EnableDefaultCompileItems>
  </PropertyGroup>
  <ItemGroup>
  </ItemGroup>

  <ItemGroup>
    <!-- Compile map: generated by tools/inventory/ejlive_inventory.py curation.
         Explicit list, no globs: every source file in this project is either compiled here
         or linked reference under src/_reference/. -->
  </ItemGroup>

  <ItemGroup>
    <!-- Compile map: generated by tools/inventory/ejlive_inventory.py curation.
         Explicit list, no globs: every source file in this project is either compiled here
         or linked reference under src/_reference/. Models/ServerModels.cs was archived under
         src/_reference/uncompiled/EJLive.Server.WinForms/Models/ (Wave 2 / D-01) — its
         partial classes (`ServerEngine`, `ReportExportEngine`, `UIBinder`,
         `OperationalStateStore`, `OperationalReportCatalogService`) lived in
         namespace EJLive.Core.Engine and duplicated Core's owners, and its
         trailing `TerminalLiveSummaryCanonical` / `TerminalCashStatusCanonical`
         declarations were missing their opening braces (CS1519/CS1001).
         Re-implement any Business-side need as extension methods over the
         canonical Core types, never as cross-assembly `partial`. -->
    <Compile Include="ATMCardPanel.cs" />
    <Compile Include="Program.cs" />
    <Compile Include="PromptTextDialog.cs" />
    <Compile Include="Properties\AssemblyInfo.cs" />
    <Compile Include="ServerMainForm.cs" />
    <Compile Include="ServerMainForm.Designer.cs" />
    <Compile Include="JournalStudioForm.cs" />
    <Compile Include="JournalStudioForm.Designer.cs" />
    <Compile Include="JournalStudioBulkAnalyzer.cs" />
    <!-- Wave 6 / C-34 — monitoring unified with the central server: the whole
         EJLive.Monitoring.WinForms surface (MainDashboardForm + designer + resx)
         moved here as Monitoring/MonitoringConsoleForm and is hosted as the
         "NOC Monitoring" tab of ServerMainForm. One console, one codebase, one
         host process. The retired EJLive.Monitoring.WinForms host (Program.cs,
         AssemblyInfo.cs, app.config, packages.config) is archived under
         src/_reference/uncompiled/EJLive.Monitoring.WinForms/retired-host/ and
         its csproj shell under src/_reference/csproj-shells/.
         Models/DashboardModels.cs was archived with it, not ported: it declared
         TerminalSnapshot/DashboardSnapshot/AlertEntry/CashStatusSummary with zero
         compiled consumers — the console renders the canonical
         EJLive.Core.Models.TerminalLiveSummary / TerminalCashStatus instead
         (C-27: no dead façade is promoted). -->
    <Compile Include="Monitoring\MonitoringConsoleForm.cs" />
    <Compile Include="Monitoring\MonitoringConsoleForm.Designer.cs" />
    <EmbeddedResource Include="ServerMainForm.resx" />
    <EmbeddedResource Include="Monitoring\MonitoringConsoleForm.resx" />
  </ItemGroup>
</Project>
